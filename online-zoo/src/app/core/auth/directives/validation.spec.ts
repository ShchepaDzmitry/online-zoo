import { Validation } from './validation';

describe('Validation', () => {
  it('should create an instance', () => {
    const directive = new Validation();
    expect(directive).toBeTruthy();
  });
});
